build:
  gpu: true
  system_packages:
    - ffmpeg
  python_version: "3.10"
  python_requirements: "torch torchvision torchaudio replicate"  # ✅ Converted to a single string

predict: "neural_style.py:Predictor"  # ✅ Enclosed in quotes for proper YAML interpretation
