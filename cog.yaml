build:
  gpu: true
  system_packages:
    - ffmpeg
  python_version: "3.10"
  python_requirements: requirements.txt  # ✅ No quotes, just the filename

predict: neural_style.py:Predictor  # ✅ Properly formatted reference
