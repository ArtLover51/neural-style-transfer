build:
  gpu: true
  system_packages:
    - ffmpeg  # ✅ No unnecessary quotes
  python_version: "3.10"
  python_requirements:
    - torch
    - torchvision
    - torchaudio
    - replicate

predict: neural_style.py:Predictor  # ✅ Correct format without quotation marks

