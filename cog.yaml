build:
  system_packages:
    - ffmpeg
    - libjpeg-turbo-dev  # ✅ Ensures image processing works
    - libpng-dev
  python_version: "3.10"
  python_requirements: requirements.txt

predict: neural_style.py:Predictor
